import numpy as np\nimport pandas as pd\nimport matplotlib.pyplot as plt\nfrom sklearn.ensemble import RandomForestClassifier\nfrom sklearn.model_selection import train_test_split\nfrom sklearn.metrics import accuracy_score, precision_score, recall_score, f1_score, roc_auc_score\nfrom sklearn.preprocessing import StandardScaler\nimport joblib\n\n# Synthetic Data Generation\ndef generate_synthetic_data(num_samples=1000):\n    np.random.seed(42)  # For reproducibility\n    features = np.random.rand(num_samples, 5)  # 5 features for simplicity\n    labels = (features.sum(axis=1) + np.random.rand(num_samples) > 2.5).astype(int)  # Binary classification\n    return features, labels\n\n# Data Preprocessing and Feature Scaling\ndef preprocess_data(features, labels):\n    X_train, X_test, y_train, y_test = train_test_split(features, labels, test_size=0.2, random_state=42)\n    scaler = StandardScaler()\n    X_train = scaler.fit_transform(X_train)\n    X_test = scaler.transform(X_test)\n    return X_train, X_test, y_train, y_test, scaler\n\n# Model Training and Evaluation\ndef train_and_evaluate(X_train, y_train, X_test, y_test):\n    model = RandomForestClassifier(n_estimators=100, random_state=42)\n    model.fit(X_train, y_train)\n    y_pred = model.predict(X_test)\n\n    # Metrics calculation\n    accuracy = accuracy_score(y_test, y_pred)\n    precision = precision_score(y_test, y_pred)\n    recall = recall_score(y_test, y_pred)\n    f1 = f1_score(y_test, y_pred)\n    roc_auc = roc_auc_score(y_test, model.predict_proba(X_test)[:, 1])\n\n    print(f'Accuracy: {accuracy:.2f}, Precision: {precision:.2f}, Recall: {recall:.2f}, F1: {f1:.2f}, ROC-AUC: {roc_auc:.2f}')\n    return model\n\n# Feature Importance Analysis\def feature_importance_analysis(model):\n    importances = model.feature_importances_\n    indices = np.argsort(importances)[::-1]\n    print('Feature ranking:')\n    for f in range(len(importances)):\n        print(f'{f + 1}. Feature {indices[f]}: {importances[indices[f]]:.4f}')\n\n# Model Serialization\def save_model(model, scaler, filename='coffee_disease_model.pkl'):\n    joblib.dump((model, scaler), filename)\n    print(f'Model saved as {filename}')\n\n# Complete Example Usage\ndef main():\n    features, labels = generate_synthetic_data()\n    X_train, X_test, y_train, y_test, scaler = preprocess_data(features, labels)\n    model = train_and_evaluate(X_train, y_train, X_test, y_test)\n    feature_importance_analysis(model)\n    save_model(model, scaler)\n\nif __name__ == '__main__':\n    main()\n