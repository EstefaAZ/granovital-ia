# =============================================================
# requirements.txt — GranoVital IA
# Instalar con: pip install -r requirements.txt
# =============================================================

# Framework web
fastapi==0.115.0
uvicorn[standard]==0.30.6

# Base de datos
sqlalchemy==2.0.35
pymysql==1.1.1
alembic==1.13.3

# Autenticación y seguridad
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
bcrypt==4.2.0

# Google OAuth
google-auth==2.35.0
google-auth-oauthlib==1.2.1
google-auth-httplib2==0.2.0

# Validación de datos
pydantic==2.9.2
pydantic-settings==2.5.2
email-validator==2.2.0

# Variables de entorno
python-dotenv==1.0.1

# Redis (caché de sesiones y tokens)
redis==5.1.1

# HTTP client (para pruebas y consumo de APIs externas)
httpx==0.27.2

# Utilidades
python-multipart==0.0.12  # Para subida de archivos (imágenes IA)

# Testing
pytest==8.3.3
pytest-asyncio==0.24.0
pytest-cov==5.0.0
